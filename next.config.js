/** @type {import('next').NextConfig} */
const nextConfig = {
    // Comment out output and basePath for local development
    // output:'export',
    // basePath:'/portfolio',
    basePath:'',
};

export default nextConfig;
